/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
/*
package club.paddle.experience;

import java.io.IOException;
import java.net.URL;
import java.util.ResourceBundle;
import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.fxml.Initializable;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.DatePicker;
import javafx.scene.control.SplitMenuButton;
import javafx.stage.Stage;*/

/**
 * FXML Controller class
 *
 * @author stefa
 */
/*public class ReservarUnaPistaController implements Initializable {

    @FXML
    private DatePicker calendario;
    @FXML
    private SplitMenuButton fecha1;
    @FXML
    private SplitMenuButton fecha2;
    @FXML
    private SplitMenuButton fecha3;
    @FXML
    private SplitMenuButton fecha4;
    @FXML
    private Button botoncancelar;
    @FXML
    private Button botonreservar;
    
    private ImageView img;
    
    private Stage primaryStage;
  
    public void initStage(Stage stage) {
       
        primaryStage = stage;
        primaryStage.setTitle("CLUB PADDLE EXPERIENCE");
        stage.setMaxHeight(400.0);
        stage.setMaxWidth(600.0);
        stage.setMinHeight(400.0);
        stage.setMinWidth(600.0);
        Image image = new image("/imagenes/external-content-duckduckgo.com.png");
        img.setInage(image);
    
    }*/
    
    //if pista seleccionada boton reservar habilitado

    /**
     * Initializes the controller class.
     * @param url
     * @param rb
     */
    /*@Override
    public void initialize(URL url, ResourceBundle rb) {
        // TODO
    } 
    
    @FXML
    private void cambioVentanaBotones(ActionEvent event) {
         try{
            if(event.getSource() == botoncancelar){ FXMLLoader miCargador = new FXMLLoader(getClass().getResource("pantallaprincipal.fxml"));
            Parent root = (Parent) miCargador.load();
            miCargador.<PantallaprincipalController>getController().initStage(primaryStage);
            Scene scene = new Scene(root);
            String css = this.getClass().getResource("cpe.css").toExternalForm();
            scene.getStylesheets().add(css);
            primaryStage.setScene(scene);
            primaryStage.show();}
            
            if(event.getSource() == ){ FXMLLoader miCargador = new FXMLLoader(getClass().getResource(""));
            Parent root = (Parent) miCargador.load();
            miCargador.<>getController().initStage(primaryStage);
            Scene scene = new Scene(root);
            primaryStage.setScene(scene);
            primaryStage.show();}
            
             }catch(IOException e){
            
        } 
    }
    
}*/
